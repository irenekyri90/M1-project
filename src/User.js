"use strict";

class User {
  constructor(name, signatureCocktail, email, password) {
    this.name = name;
    this.signatureCocktail = signatureCocktail;
    this.email = email;
    this.password = password;
  }
}